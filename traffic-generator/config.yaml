apiVersion: v1
kind: ConfigMap
metadata:
  name: locust-config
data:
  config.json: |
    {
        "tasks": [
            {
                "name": "Fetch Home Page",
                "method": "GET",
                "endpoint": "/",
                "frequency": 4
            }
        ]
    }
